
# 1 Analyse zweier Algorithmen

![[98_ESA/image/Pasted image 20241115095017.png]]

## 1.1 Welche Ausgabe liefert SumOfTwo mit der Eingabe A = (1; 4; 8; 16; 0; 3; 2)?

true, da 0 is ein element in A 
## 1.2 Welche Ausgabe liefert SumOfTwoSorted mit der Eingabe A = (0; 1; 2; 4; 7; 8; 16)?

true, da 0 is ein element in A 

## 1.3 Erläutern Sie Zeile 5/6 und Zeile 27/28.

 Zeile 5/6
```
if A[i] = A[j] + A[k] then
	found  <- true
```

if value of `A[i]` is the sum of  `A[j]` and  `A[k]`   , it prove the value of i-th element is the summe of other two element in the Array

 
Zeile 27/28
```
k = BinarySearch(diff; A; 1; j)
if k > 0 then
```

k = BinarySearch(diff; A; 1; j)
found whether the value any element  in the Array A in the index range 1 to j  is equal to  the value of variant diff 
if it can be found , return the index  of this found element. This index should be greater then 0 
if it's not be found, return 0 


## 1.4 Bestimmen Sie jeweils die Eingabegroße / Eingabegrößen (`??`)

$a_i \in A$ and  $n_i \in N_0$

## 1.5 Welches ist jeweils die grundlegende Operation zur Bestimmung der Zeitkomplexität?

SumOfTwo(A, n): 
Zeile 5 
```
if A[i] = A[j] + A[k] then
```


---
SumOfTwoSorted(A, n): 
Zeile 25 
```
if diff > A[j] then
```



## 1.6 Wie häufig wird die grundlegende Operation im schlechtesten Fall ausgeführt?

SumOfTwo(A, n): 
Anzahl der Ausführung: $n^2$
Wenn the value of the last element in the Array ist 0 and the value of element in the Array A is not the sum of any other two elements (except 0) 

---

SumOfTwoSorted(A, n): 
Anzahl der Ausführung:  n 

?? Should be considered whether the following part is executed 

Zeile 32 to 33
```
if not found then
	return FALSE
```




## 1.7 Wie häufig wird Zeile 5 bzw. Zeile 25 im schlechtesten Fall ausgeführt?

SumOfTwo(A, n): 
Anzahl der Ausführung: $n^2$
Wenn the value of the last element in the Array ist 0 and the value of element in the Array A is not the sum of any other two elements (except 0) 


---

SumOfTwoSorted(A, n): 
Anzahl der Ausführung: $1+2+...+ n = (n*(n+1))/2$

## 1.8 Geben Sie eine Beispieleingabe der Lange 6 an, bei der Zeile 26 ausgeführt wird.


![[98_ESA/image/Pasted image 20241115095017.png]]

Beispiel: A = (1, 1, 3, 3, 3, 8) 

Zeile 25-26
```
if diff > A[j] then 
	Break // End Loop for j 
```



## 1.9 Geben Sie jeweils die Zeitkomplexitat für den schlechtesten Fall in asymptotischer Notation an (ohne Beweis).

SumOfTwo(A, n):  $O(n^2)$


SumOfTwoSorted(A, n): $O((n*(n+1))/2)$ ~= $O(n^2) + O(n) \in O(n^2)$

## 1.10 Gibt es eine bessere Lösung für den unsortierten Fall, welche Zeitkomplexität hat sie?


![[98_ESA/image/Pasted image 20241115170729.png]]

```
for c ← 1 to n // pick next candidate
	if A[c] = 0 // check for zero, because then…
		return TRUE // …there will always be a sum

for c ← 1 to n // pick next candidate
	c_is_sum ← FALSE // assume c has no summands
	for a ← 1 to n // all numbers for first summand
		for b ← a to n // only needed numbers for second summand
			if A[c] = A[a] + A[b] // test sum
				c_is_sum ← TRUE // confirmed!
	if not c_is_sum // we really found no sum for c
		return FALSE // we’re done

return TRUE // all candidates were sums, also done
```


# 2 Asymptotische Notation 

![[98_ESA/image/Pasted image 20241115172622.png]]


## 2.1 
Geben Sie für jede der folgenden Funktionen an, in welcher asymptotischen Klasse sie liegt.

a: $O(N^2)$
b: $O(N)$
c: $(3n^2 - n)/2$  $\in O(n^2)$
d: -21 $\in O(1)$
e:  $O(2^n)$

## 2.2 

f: 
f(n) = $n(log_2(n) - 1) + 8$  = $nlog_2(n) - n + 8$  

Klassenzuordnung: $O(1) \in O(n) \in O(nlog_2(n)) v\in O(n^2)$   

Komplexität von f(n)  =   $O(nlog_2(n)) - O(n) + O(1)$    $\in O(nlog_2(n))$ 

g: 
$f(n)  $\in O(nlog_2(n))$


h: 
$f(n)  $\in O(n^2)$   and also $f(n)  $\in O(nlog_2(n))$


# 3 Rekursive Algorithmen 

![[98_ESA/image/Pasted image 20241115220135.png]]

## 3.1 a

Algorithmus 1.3 (MinRek1)

`MinREK1(A, 1, 5)  ->  Min( A[1], MinREK1(A, 2, 5) ) ` -> 
`MinREK1(A, 2, 5) -> Min( A[2], MinREK1(A, 3, 5) )` -> 
 `MinREK1(A, 3, 5) -> Min( A[3], MinREK1(A, 4, 5) ) ` -> 
`MinREK1(A, 4, 5)`  -> `Min( A[4], MinREK1(A, 5, 5) ) `

`MinREK1(A, 5, 5) `-> return 21 
MinREK1(A, 4, 5) = `Min( A[4], MinREK1(A, 5, 5) )`  return 9
MinREK1(A, 3, 5)  = `Min( A[3], MinREK1(A, 4, 5) ) ` return 2 
MinREK1(A, 2, 5) = `Min( A[2], MinREK1(A, 3, 5) )`  return 2 
MinREK1(A, 1, 5)  =  ` Min( A[1], MinREK1(A, 2, 5) ) `  return 2 


---


Algorithmus 1.4 (MinRek2)


`MinREK1(A, 1, 5)  
- m1 <-  `MinREK1(A, 1, 3)  
	- `MinREK1(A, 1, 2) 
		- `MinREK1(A, 1, 1)  = A[1]`
		-  `MinREK1(A, 2, 2) = A[2]`
		- `Min ( A[1], A[2])` = 3
	- `MinREK1(A, 2, 3) 
		- `MinREK1(A, 2, 2)  = A[2]`
		- `MinREK1(A, 3, 3) = A[3]`
		- `Min ( A[2], A[3])` = 2
	- `MinREK1(A, 1, 3)  = Min(3, 2)  =2 `
- m2 <-  `MinREK1(A, 4, 5) 
	- `MinREK1(A, 4, 4)  = A[4]`
	- `MinREK1(A, 5, 5) = A[5]`
	- `Min ( A[4], A[5])` = 9
- `Min ( MinREK1(A, 1, 3), MinREK1(A, 4, 5))`  = 2 

## 3.2 b 

die Zeitkomplexitat beider Algorithmen: $O(n)$


Wie oft wird die Methode Min auf gerufen: 
Algorithmus 1.3 (MinRek1)  : n-1 Mal
Algorithmus 1.4 (MinRek2)  : n-1 Mal




## 3.3 c
Liefern beide Lösungen in jedem Fall das gleiche, korrekte Ergebnis: Ja,  beide Lösungen liefern das gleiche, korrekte Ergebnis 


## 3.4 d
Welche der beiden Lösungen ist effzienter?
??  (问老师)
## 3.5 e 
Gibt es effzientere Lösungen? Wenn Ja, geben Sie den Algorithmus an, wenn nein, begründen Sie dies.
??  (问老师)





# 4 Rekursiver Algorithmus

![[98_ESA/image/Pasted image 20241115224546.png]]

## 4.1 a

Rekursionsgleichung


--- 

Geben Sie an, welches die für die Zeitkomplexit at relevante Eingabegröße ist.  ???  (问老师 )





## 4.2 
Bestimmen Sie die Zeitkomplexität des Algorithmus RekAlg9.


