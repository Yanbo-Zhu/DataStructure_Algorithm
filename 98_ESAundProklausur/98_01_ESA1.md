

0

Gruppenmitglieder 
- Yanbo Zhu
- Göker Çakir
- Lamiaa El Awane

Amir Hammoud gehört nicht zu unserem Team

# 1 Analyse zweier Algorithmen

![[98_ESAundProklausur/image/Pasted image 20241115095017.png]]

## 1.1 Algorithmus 1.1

![[98_ESAundProklausur/image/Pasted image 20250120161625.png]]


einfach
- Algorithmus sucht stumpf Summen
- sucht zuviel
- Zeitkomplexität im schlechtesten Fall $W(n) \in O(n^3)$
- Eingabegrößen A und n
	- n vergessen nicht so schlimm
	- A vergessen schlimm



## 1.2 Algorithmus 1.2

![[98_ESAundProklausur/image/Pasted image 20250120164439.png]]

nur 2 Fälle möglich:
- 0 enthalten, ein Durchlauf ➔ also n-mal Binary Search
- keine 0 enthalten ➔ bricht sofort ab (ist ja sortiert, also kann schon erstes = kleinstes Element keine Summe sein)

Zeitkomplexität im schlechtesten Fall: $W(n) \in O(nlogn)$

## 1.3 a
Welche Ausgabe liefert SumOfTwo mit der Eingabe A = (1; 4; 8; 16; 0; 3; 2)

true, da 0 is ein element in A 
## 1.4 b
Welche Ausgabe liefert SumOfTwoSorted mit der Eingabe A = (0; 1; 2; 4; 7; 8; 16)

true, da 0 is ein element in A 

## 1.5 c
Erläutern Sie Zeile 5/6 und Zeile 27/28.

 Zeile 5/6
```
if A[i] = A[j] + A[k] then
	found  <- true
```

if value of `A[i]` is the sum of  `A[j]` and  `A[k]`   , it proves the value of i-th element is the sum of other two element in the Array

---

Zeile 27/28
```
k = BinarySearch(diff; A; 1; j)
if k > 0 then
```

k = BinarySearch(diff; A; 1; j)
find whether the value any element  in the Array A in the index range 1 to j  is equal to  the value of variant diff 
if it can be found , return the index  of this found element. This index should be greater then 0 
if it's not be found, return 0 


## 1.6 d: Eingabegrößen 
Bestimmen Sie jeweils die Eingabegroße / Eingabegrößen 

Antwort: Array mit Länge n

$A= (a_1, a_2, ..., a_n)$

$a_i \in A$ and  $n_i \in N_0$


## 1.7 e: grundlegende Operation

Welches ist jeweils die grundlegende Operation zur Bestimmung der Zeitkomplexität?

SumOfTwo(A, n): 
Auswertung der Bedingung in Zeile 5 
```
if A[i] = A[j] + A[k] then
```


---
SumOfTwoSorted(A, n): 

我自己写的 (这是错的): 
Auswertung der Bedingung in Zeile 25 
```
if diff > A[j] then    
```


这是对的 : Zeile 27 
```
k = BinarySearch(diff; A; 1; j)
```


## 1.8 f
Wie häufig wird die grundlegende Operation im schlechtesten Fall ausgeführt?

SumOfTwo(A, n): 

grundlegende Operation: 
 Zeile 5/6
```
if A[i] = A[j] + A[k] then
	found  <- true
```



我自己回答的 (这是错的 ):

Anzahl der Ausführung der die grundlegende Operation: $n^2$
Wenn the value of the last element in the Array ist 0 and the value of element in the Array A is not the sum of any other two elements (except 0) 


正确答案: 

Anzahl der Ausführung der die grundlegende Operation: $n^3$
Wenn everyelement ist 0 in the arreay

---

SumOfTwoSorted(A, n):  


正确的 
grundlegende Operation: 
Zeile 27 
```
k = BinarySearch(diff; A; 1; j)
```

Anzahl der Ausführung der die grundlegende Operation:  n 





这是我自己回答的 是错的 
Zeile 25/26
```
if diff > A[j] then
	break// End loop for j
```


Anzahl der Ausführung der die grundlegende Operation:  $n(n+1)/2$ 


## 1.9 g (由于题设不对, 这题不用再做了)
Wie häufig wird Zeile 5 bzw. Zeile 29 im schlechtesten Fall ausgeführt?

SumOfTwo(A, n): 
Anzahl der Ausführung: $n^2$
Wenn the value of the last element in the Array ist 0 and the value of element in the Array A is not the sum of any other two elements (except 0) 

Zeile 5/6
```
if A[i] = A[j] + A[k] then
	found  <- true
```


---

SumOfTwoSorted(A, n): 
Anzahl der Ausführung: $1+2+...+ n = (n*(n+1))/2$
Zeile 27/28/29/30
```
k = BinarySearch(diff; A; 1; j)
if k > 0 then
	found <- true;
	break// End loop for j
```


## 1.10 h (由于题设不对, 这题不用再做了)
Geben Sie eine Beispieleingabe der Lange 6 an, bei der Zeile 26 ausgeführt wird.


![[98_ESAundProklausur/image/Pasted image 20241115095017.png]]

Zeile 25-26
```
if diff > A[j] then 
	Break // End Loop for j 
```


Beispiel: A = (1, 1, 3, 3, 3, 8) 



## 1.11 i
Geben Sie jeweils die Zeitkomplexitat für den schlechtesten Fall in asymptotischer Notation an (ohne Beweis).

SumOfTwo(A, n):  $O(n^2)$  自己做的 错了
正确的  $O(n^3)$

---


SumOfTwoSorted(A, n): $O((n*(n+1))/2) \in  O(n^2) + O(n) \in O(n^2)$  自己做的 错了
正确的  $O(n log(n))$

Algorithmus nur 2 Fälle möglich:
- 0 enthalten, ein Durchlauf ➔ also n-mal Binary Search
- keine 0 enthalten ➔ bricht sofort ab (ist ja sortiert, also kann schon erstes = kleinstes Element keine Summe sein)

Zeitkomplexität im schlechtesten Fall: $W(n) \in O(nlogn)$


## 1.12 j
Gibt es eine bessere Lösung für den unsortierten Fall, welche Zeitkomplexität hat sie?

Old Version 
![[98_ESAundProklausur/image/Pasted image 20241115170729.png]]


Advanced Version 
```c
SumOfTwo_advanced(A,n)
for c ← 1 to n // pick next candidate
	if A[c] = 0 // check for zero, because then…
		return TRUE // …there will always be a sum

for c ← 1 to n // pick next candidate
	c_is_sum ← FALSE // assume c has no summands
	for a ← 1 to n // all numbers for first summand
		for b ← a to n // only needed numbers for second summand
			if A[c] = A[a] + A[b] // test sum
				c_is_sum ← TRUE // confirmed!
	if not c_is_sum // we really found no sum for c
		return FALSE // we’re done

return TRUE // all candidates were sums, also done
```

Zeitkomplexität: $O(n) + O (n(n+1) /2 ) \subset O(n^2)$ 

# 2 Asymptotische Notation 

![[98_ESAundProklausur/image/Pasted image 20241115172622.png]]

## 2.1 a-e: asymptotischen Klasse
Geben Sie für jede der folgenden Funktionen an, in welcher asymptotischen Klasse sie liegt.

a: $\Theta(n^2)$
b: $\Theta(n)$
c: $(2n^2 + n)/2$  $\in O(n^2)$
d: 21n $\in O(n)$
e:  $O(2^n)$

## 2.2 f

f(n) = $n(log_2(n) - 1) + 8$  = $nlog_2(n) - n + 8$  
Klassenzuordnung: $O(1) \in O(n) \in O(nlog_2(n)) v\in O(n^2)$   

Zu beweisen: 
Für alle $n \ge n_0$ : $f(n) \le c \cdot nlog(n)$

Es gilt für $n \ge 9$
$n(log_2(n) - 1) + 8$  = $nlog_2(n) + 8-n$  $\le nlog_2(n)$ wenn $n \ge 9$

Wähle c = 1 und $n_0 = 9$
Also $f(n) \in O(nlog_2(n))$

## 2.3 g


Zu beweisen: 
Für alle $n \ge n_0$ : $f(n) \ge c \cdot n$

Es gilt für $n \ge 4$
$n(log_2(n) - 1) + 8 \ge n(log_2(n)-1)  \ge n$, weil $(log_2(n)-1)$ immer > 1, wenn $n \ge 4$

Wähle c = 1 und $n_0$ = 4
Also $f(n) \in \Omega(n)$


---

正确答案 为 

Zu beweisen: 
Für alle $n \ge n_0$ : $f(n) \ge c \cdot n(log_2 n)$


![[98_ESAundProklausur/image/Pasted image 20250120165511.png]]


$\frac{1}{2}n(log_2 n) -n  + 8$ = $\frac{1}{2}n ( log_2 n -2) +8$

$log_2 n -2  > -1$  , ab n=1

然后又 + 8 

所以 $\frac{1}{2}n ( log_2 n -2) +8$  immer > 0 , ab n=1 


## 2.4 h

$f(n) \in \Theta(nlog_2(n))$
Da  $f(n) \in max ( O(nlog_2(n)) , \Omega(n))$


正确答案 
![[98_ESAundProklausur/image/Pasted image 20250120170419.png]]



# 3 Rekursive Algorithmen 

![[98_ESAundProklausur/image/Pasted image 20241117181947.png]]

• links entarteter 退化的 Baum (Liste) 
• rechts binärer Baum
• beide aber dasselbe Ergebnis 2
• suchen einfach das Minimum aller Werte rekursiv
• man muss eh jedes Element einmal ansehen, also wäre linear durchsuchen einfacher

## 3.1 a: Analysieren Sie beide Algorithmen.

Welche Aufrufe geschehen jeweils fur die Eingabe A = (3, 7, 2, 9, 2, 1)? Geben Sie fur jeden Aufruf von MinRek1 bzw. MinRek2 die Parameter und den Rückgabewert an.


### 3.1.1 Algorithmus 1.3 (MinRek1)

先从左到右 拆成一个个小的元素 , 然后在右到左 一个个合并来找最小的元素 

`MinREK1(A, 1, 5)  ->  Min( A[1], MinREK1(A, 2, 5) ) ` -> 
`MinREK1(A, 2, 5) -> Min( A[2], MinREK1(A, 3, 5) )` -> 
 `MinREK1(A, 3, 5) -> Min( A[3], MinREK1(A, 4, 5) ) ` -> 
`MinREK1(A, 4, 5)`  -> `Min( A[4], MinREK1(A, 5, 5) ) `

`MinREK1(A, 5, 5) `-> return 21 
MinREK1(A, 4, 5) = `Min( A[4], MinREK1(A, 5, 5) )`  return 9
MinREK1(A, 3, 5)  = `Min( A[3], MinREK1(A, 4, 5) ) ` return 2 
MinREK1(A, 2, 5) = `Min( A[2], MinREK1(A, 3, 5) )`  return 2 
MinREK1(A, 1, 5)  =  ` Min( A[1], MinREK1(A, 2, 5) ) `  return 2 


![[98_ESAundProklausur/image/Pasted image 20250125164030.png]]


---

### 3.1.2 Algorithmus 1.4 (MinRek2)

从中间均分, 拆成一个个小的集合 ,  在左右分开合并, 合并的同时找最小的元素 

![[98_ESAundProklausur/image/Pasted image 20250125164404.png]]

`MinREK1(A, 1, 5)  
- m1 <-  `MinREK1(A, 1, 3)  
	- `MinREK1(A, 1, 2) 
		- `MinREK1(A, 1, 1)  = A[1]`
		-  `MinREK1(A, 2, 2) = A[2]`
		- `Min ( A[1], A[2])` = 3
	- `MinREK1(A, 2, 3) 
		- `MinREK1(A, 2, 2)  = A[2]`
		- `MinREK1(A, 3, 3) = A[3]`
		- `Min ( A[2], A[3])` = 2
	- `MinREK1(A, 1, 3)  = Min(3, 2)  =2 `
- m2 <-  `MinREK1(A, 4, 5) 
	- `MinREK1(A, 4, 4)  = A[4]`
	- `MinREK1(A, 5, 5) = A[5]`
	- `Min ( A[4], A[5])` = 9
- `Min ( MinREK1(A, 1, 3), MinREK1(A, 4, 5))`  = 2 

## 3.2 b Zeitkomplexität

Bestimmen Sie die Zeitkomplexitder beider Algorithmen. Wie oft wird die Methode Min aufgerufen? Geben Sie dies als alleinige Funktion von n an, falls möglich.


### 3.2.1 Wie oft wird die Methode Min auf gerufen: 
Algorithmus 1.3 (MinRek1)  : n-1 Mal
Algorithmus 1.4 (MinRek2)  : (n-1)/2 Mal  这里回答错了, Zweite Algorithmen 中 Min methode 也是被执行了 n-1 次

![[98_ESAundProklausur/image/Pasted image 20250125165223.png]]


### 3.2.2 die Zeitkomplexität 

下面是我胡写的 根本没有按照 正确的方法计算 
die Zeitkomplexität 
Erste Algorithmen: $\Theta(n)$
Zweite Algorithmen: $\Theta(log(n))$, da sie den Bereich halbiert und für jeden Teilbereich einen Vergleich durchführt.     这里回答错了, Zweite Algorithmen 也是 

---

正确的计算 Zeitkomplexität T(n) 方式 

1 MinRek1
![[98_ESAundProklausur/image/Pasted image 20250125165426.png]]

这个无法按照 Master-Theorem 公式计算 


2 MinRek2

![[98_ESAundProklausur/image/Pasted image 20250125165419.png]]

b = 2, c=2 , k =0 

a=ln(2)/ln(2) = 1

$T(n) \in \Theta(n)$


## 3.3 c: Korrekter Rückgabewert
Liefern beide Lösungen in jedem Fall das gleiche, korrekte Ergebnis? 
Antwort:  Beide Algorithmen liefern das gleiche, korrekte Ergebnis, da sie das Minimum über die gesamte Eingabe finden.
## 3.4 d Effizienterer Algorithmus
Welche der beiden Lösungen ist effzienter?
Antwort: MinRek2 ist effizienter, da es die Eingabe in logaritmischer Tiefe verarbeitet, während MinRek1 linear alle Elemente prüft.

## 3.5 e: Gibt es effizientere Lösungen
Gibt es effzientere Lösungen? Wenn Ja, geben Sie den Algorithmus an, wenn nein, begründen Sie dies.
Antwort:   Ja, ein iterativer Ansatz mitO(n) ist effizienter:

```
def find_min(A):
	min_value = A[0]
	for i in range(1, len(A)):
		   if A[i] < min_value:
			   min_value = A[i]

	return min_value
```


# 4 Rekursiver Algorithmus

![[98_ESAundProklausur/image/Pasted image 20241117182041.png]]

## 4.1 a: Rekursionsgleichung

Stellen Sie die Rekursionsgleichung zur Bestimmung der Zeitkomplexit des Algorithmus RekAlg9 in Abhängigkeit von der Eingabegröße auf und geben Sie an, welches die für die Zeitkomplexität relevante Eingabegröße ist. (Vernachlassigen Sie dabei die Gaussklammern.)

relevante Eingabegröße ist n 

Rekursionsgleichung
1, für n=1 
$2T(3n/2) + n^2$, für $n \ge2$

---
正确答案

f(n) 不是 $n^2$ , 而是 2n

因为  51 to 52 是 h(k) 被加了 n 次 , 53 to 54 是 h(k) 被加了 n 次. 两个独立的循环, 不是嵌套的.     而且每次计算的复杂度 都是 $\Theta (1)$, 所以一共是2n 



## 4.2 b: Zeitkomplexität

Bestimmen Sie die Zeitkomplexität des Algorithmus RekAlg9.

b=2
c= 3/2
因为 f(n) = $n^2$      k=2

$2 \cdot (1/(3/2))^2$ = $8/9$ $< 1$

Also 
$T(n) \in \Theta(n^k) = \Theta(n^2)$


--- 
正确答案

b=2
c= 3/2
因为 f(n) = $2n$     k=1

$2 \cdot (1/(3/2))^1 = \frac{4}{3} >  1$


$a = ln(2)/ln(3/2) = \log_{3/2}2$
$T(n) \in \Theta(n^a) = \Theta(n^{3.409})$


![[98_ESAundProklausur/image/Pasted image 20250125163220.png]]

